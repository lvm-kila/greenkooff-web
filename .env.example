# Runtime provider selection (default mock)
NEXT_PUBLIC_AUTH_PROVIDER=mock
NEXT_PUBLIC_DATA_PROVIDER=mock
NEXT_PUBLIC_PAYMENTS_PROVIDER=mock

# Cognito (future phase)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=
NEXT_PUBLIC_COGNITO_REGION=us-east-1

# DynamoDB (future phase)
AWS_REGION=us-east-1
DYNAMODB_SUBSCRIPTIONS_TABLE=
DYNAMODB_BILLING_EVENTS_TABLE=
DYNAMODB_USERS_TABLE=

# Transbank Oneclick (future phase)
TRANSBANK_ENV=integration
TRANSBANK_COMMERCE_CODE=
TRANSBANK_API_KEY=

# Amplify SSR runtime (future phase)
AMPLIFY_APP_ID=
AMPLIFY_BRANCH=
